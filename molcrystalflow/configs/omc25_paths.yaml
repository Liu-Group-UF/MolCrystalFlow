paths:
  root_dir: "."  # Project root (relative path)
  log_dir: ${paths.root_dir}/runs-omc25-mcf/${experiment.wandb.name}
  wandb_dir: ${paths.root_dir}/runs-omc25-mcf
  ckpt_dir: ${paths.log_dir}/ckpt
  inference_dir: ${paths.log_dir}/inference
  data_dir: ${paths.root_dir}/processed-OMC25