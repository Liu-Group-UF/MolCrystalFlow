paths:
  root_dir: "."  # Project root (relative path)
  log_dir: ${paths.root_dir}/runs/${experiment.wandb.name}
  wandb_dir: ${paths.root_dir}/runs
  ckpt_dir: ${paths.log_dir}/ckpt
  inference_dir: ${paths.log_dir}/inference
  data_dir: ${paths.root_dir}/processed-thurlemann